# Inspect Existing Skill

Read the existing skill before changing anything.

Inspect:

- frontmatter `name` and `description`
- trigger language
- workflow sections
- bundled resources
- scripts, templates, evals, or references
- assumptions about tools or environment

Preserve source behavior unless the user asks for redesign. Call out ambiguous or outdated sections before transforming them.
